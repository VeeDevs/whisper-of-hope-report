# Example environment variables for local development / reference
# Do NOT commit real secrets. Copy this to `.env` locally if you need to run the app.
# Vite loads env vars prefixed with VITE_ at build time.

# Required for Supabase integration
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJI...your-anon-key..._example

# Optional: other VITE_ prefixed variables can be added here
